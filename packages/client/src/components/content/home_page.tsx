import React, { FunctionComponent as FC } from 'react';

const Homepage: FC = () => (
  <div style={{ width: '100%', height: '500px', backgroundColor: 'magenta' }}>
    A box
  </div>
);

export default Homepage;
